-- Creates a table called first_table in the current database
-- id INT, name VARCHAR(256)
-- If table already exists, the script will not fail
CREATE TABLE If NOT EXISTS first_table (id INT, name VARCHAR(256));
